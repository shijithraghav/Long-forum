<%if @tags %>
console.log('tags');

 $('#search-posts').html('<%= j render @articles %>')
 <% if @articles.next_page %>
     $('.pagination').replaceWith('<%= j will_paginate @articles %>');
     console.log('second');
 <% else %>
     $(window).off('scroll');
     $('.pagination').remove();
   <% end %>

<% else %>

$('<%= j render @articles %>').insertBefore( "#infinite-scrolling" );
  console.log('first');
  <% if @articles.next_page %>
      $('.pagination').replaceWith('<%= j will_paginate @articles %>');
  <% else %>
      $(window).off('scroll');
      $('.pagination').remove();
    <% end %>
<%end %>
