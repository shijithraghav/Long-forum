<div class="ui container">    <div class = "ui segment">      <p>        <h1 class="ui left aligned green header">          <%= @article.title %>        </h1>        <div class="ui divider"></div>        <%= raw(@article.text) %>        <div class= "action">            Tags:            <%  @article.tag_list.each do |t| %>          <%=link_to t , tag_path(t) ,class: 'ui blue  label' %>            <%end%>        </div><%= link_to ('<button class = "ui right floated button">Add Sub Post</button>').html_safe , subpostnew_article_path(@article.id), data: {method: "get"}  %>        <div id="fav1">          <%= render 'favorite' %>        </div>       <%#= form_tag(controller: "articles", action: "favorite", method: "post") do %>    <%#= submit_tag "Favorite"%><%# end %>        <%= render 'article' %>        <div class ="sub_post">          <h3 class="ui left aligned blue header">            Subposts          </h3>          <% @subarticles.each do |subarticle| %>           <%= link_to subarticle.title, article_path(subarticle) %>          <div class="ui divider"></div>          <% end %>      </div> <% if @article.visibility == "private" &&current_user.id == @article.user_id %>     <div class="ui action input">       <%= form_tag url_for(invite_articles_path), :remote => true do %>         <%= select_tag "user_id", options_from_collection_for_select(@users, "id", "email") %>         <%= hidden_field_tag :article_id, @article.id %>         <%= hidden_field_tag :status, 'false' %>         <%= submit_tag "Invite" %>       <% end %>     </div>     <div id ="invited"> </div>   <% end %>        <h3 class="ui dividing header">Comments</h3>        <div class = "comments">          <%= render @article.comments %>          <%= render 'comments/form' %>        </div>        <%= link_to 'Edit', edit_article_path(@article) %> |        <%= link_to 'Back', articles_path %>      </p>    </div></div>