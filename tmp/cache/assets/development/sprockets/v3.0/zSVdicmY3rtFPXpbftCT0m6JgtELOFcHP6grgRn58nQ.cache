I"A(function() {
  $(document).on('ready page:load', function() {
    var i, len, ref, tag;
    $('#article-tags').tagit({
      fieldName: 'article[tag_list]',
      singleField: true,
      availableTags: gon.available_tags
    });
    if (gon.article_tags != null) {
      ref = gon.article_tags;
      for (i = 0, len = ref.length; i < len; i++) {
        tag = ref[i];
        $('#article-tags').tagit('createTag', tag);
      }
    }
    return $('#book_search').typeahead({
      name: "book",
      remote: "/books/autocomplete?query=%QUERY"
    });
  });

}).call(this);
:ET